# Private grid staging folder

Temporarily place the official grid here for a private deployment image build:

```text
transform-service/private-grid/GDA94_GDA2020_conformal_and_distortion.gsb
```

Do not commit the `.gsb` file. The repository root `.gitignore` ignores `*.gsb`.
