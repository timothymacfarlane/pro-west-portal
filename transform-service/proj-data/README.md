# PROJ data

Place the official ICSM NTv2 grid here for local Docker builds or mount it at runtime:

```text
GDA94_GDA2020_conformal_and_distortion.gsb
```

Do not commit the `.gsb` grid file to this repository. It is approximately 79.2 MiB and should be managed as a deployment artifact or mounted secret/volume.

Known SHA256 from the official grid file used during validation:

```text
4faecf467eaf646bc97983865ac1a4380001781ac504a13aae8a7f9da102496a
```
